@page
@model Disable2faModel
@{
    ViewData["Title"] = "Deshabilitar la autenticación de dos factores (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Esta acción solo deshabilita 2FA.</strong>
    </p>
    <p>
        Deshabilitar la autenticación de dos factores no cambia las claves utilizadas en las aplicaciones de autenticación. Si desea cambiar la clave deberia haber
        utilizado en una aplicación de autenticación que debería <a asp-page="./ResetAuthenticator">Restablecer sus claves de autenticador.</a>
    </p>
</div>

<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Desactivar 2FA</button>
    </form>
</div>
